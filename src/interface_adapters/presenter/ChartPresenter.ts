/*
Path: src/interface_adapters/presenter/ChartPresenter.ts
*/

export function formatChartOptions(raw: Record<string, unknown>): Record<string, unknown> {
  // Aquí podrías transformar datos, validar, etc. Por ahora, retorna tal cual.
  return raw
}
